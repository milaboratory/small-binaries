---
"@platforma-open/milaboratories.software-small-binaries.line-counter": patch
---

Document that line-counter counts line terminators, so a file without a trailing newline counts one fewer than an editor shows.
