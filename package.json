{
  "name": "small-binaries",
  "scripts": {
    "cleanup": "pnpm -r cleanup",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:dry-run": "turbo run test --dry-run=json",
    "changeset": "changeset",
    "version-packages": "changeset version"
  },
  "license": "UNLICENSED",
  "packageManager": "pnpm@9.14.4",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "turbo": "catalog:",
    "@changesets/cli": "catalog:"
  },
  "//pnpm": {
    "overrides": {
      "@platforma-sdk/package-builder": "file:/Users/denkoren/work/milaboratory/platforma/platforma/tools/package-builder/package.tgz"
    }
  }
}
