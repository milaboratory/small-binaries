{
  "name": "small-binaries",
  "scripts": {
      "cleanup": "turbo run cleanup",
      "build": "turbo run build",
      "test": "turbo run test",

      "prepublishOnly": "turbo run upload-packages"
  },
  "files": [
      "dist/"
  ],
  "license": "UNLICENSED",
  "packageManager": "pnpm@9.14.4",
  "engines": { "node": ">=20" },
  "devDependencies": {
      "@platforma-sdk/package-builder": "catalog:"
  },
  "pnpm": {
    "overrides": {
        "@platforma-sdk/package-builder": "file:/Users/denkoren/work/milaboratory/platforma/platforma/tools/package-builder/package.tgz"
    }
  }
}
